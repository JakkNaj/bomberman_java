package fel.cvut.cz.tiles;

import fel.cvut.cz.graphics.Assets;

/** Tile that boosts explosion radius of bomb, when stepped on it. */
public class ExplosionBoost extends Tile{
    public ExplosionBoost(int id) {
        super(Assets.explosionBoost, id);
    }
}
