package fel.cvut.cz.tiles;

import fel.cvut.cz.graphics.Assets;

/** Tile that boosts Players running speed, when stepped on it. */
public class RunningBoost extends Tile{
    public RunningBoost(int id) {
        super(Assets.runBoost, id);
    }
}
